export interface TodoItem {
  id: string;
  title: string;
  body: string;
  complete: boolean;
}
export interface TodoInput {
  id: string;
  title: string;
  body: string;
  complete: boolean;
}
